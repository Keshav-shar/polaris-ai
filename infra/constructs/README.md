# constructs/

**Purpose:** Reusable CDK constructs (for example, a Lambda with the standard log retention and alarm settings).

**May depend on:** aws-cdk-lib.

**Must not depend on:** Stack-specific wiring.

**Planned contents:**
- Add constructs only when a pattern repeats in two stacks.
