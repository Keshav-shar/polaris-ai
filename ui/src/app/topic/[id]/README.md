# [id]/

**Purpose:** Topic page: article, quiz, progress tracking.

**May depend on:** ui/src/components, ui/src/lib.

**Must not depend on:** Direct database access.

**Planned contents:**
- `page.tsx` — shows article + quiz; posts to /progress on completion (also time spent)
