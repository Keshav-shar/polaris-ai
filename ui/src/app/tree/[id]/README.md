# [id]/

**Purpose:** Tree navigation page.

**May depend on:** ui/src/components, ui/src/lib.

**Must not depend on:** Direct database access.

**Planned contents:**
- `page.tsx` — renders the tree and navigates ancestors/descendants via API (ltree queries run server-side)
