# lib/

**Purpose:** API client and contract types.

**May depend on:** Standard library, fetch.

**Must not depend on:** Components or routes.

**Planned contents:**
- `api-client.ts` — typed wrapper around the backend API
- `contract.ts` — request/response types
