# ui/

**Purpose:** Next.js frontend, deployed to Vercel Hobby. Named `ui` (the plan calls it `app`) to avoid confusion with Next's `app/` router directory.

**May depend on:** The backend HTTP contract only.

**Must not depend on:** service/src (no deep imports). Share types through a generated or published contract.

**Planned contents:**
See the sub-folders; this folder holds no files of its own.
