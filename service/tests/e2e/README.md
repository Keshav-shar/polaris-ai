# e2e/

**Purpose:** Jest + Supertest against the HTTP interface with in-memory adapters.

**May depend on:** interfaces/http, composition (test wiring).

**Must not depend on:** Live LLM calls.

**Planned contents:**
- `api/topics.routes.test.ts` and one file per route module
