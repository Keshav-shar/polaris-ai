# tests/

**Purpose:** All backend tests. Sub-folders mirror `src/` layers so the test pyramid is visible.

**May depend on:** Any layer under test, plus test doubles from fixtures.

**Must not depend on:** Production code depending on tests.

**Planned contents:**
See the sub-folders; this folder holds no files of its own.
