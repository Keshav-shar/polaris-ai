# search/

**Purpose:** Web-search adapter used by the retrieval agent.

**May depend on:** application/ports, domain.

**Must not depend on:** LLM adapters.

**Planned contents:**
- `web-search.adapter.ts` — implements `search.port.ts`
