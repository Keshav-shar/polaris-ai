# infrastructure/

**Purpose:** Adapters that implement application ports against real services.

**May depend on:** application/ports, domain, config/constants, shared, third-party SDKs.

**Must not depend on:** interfaces, application/use-cases, application/agents, application/graph. Business rules (belongs in domain).

**Planned contents:**
See the sub-folders; this folder holds no files of its own.
