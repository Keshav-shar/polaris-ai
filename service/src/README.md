# src/

**Purpose:** Backend TypeScript source, organised in layers. Dependencies point inward only: domain <- application <- infrastructure / interfaces, wired together in composition.

**May depend on:** See the layer table in ../README.md.

**Must not depend on:** Anything that reverses the dependency direction.

**Planned contents:**
See the sub-folders; this folder holds no files of its own.
